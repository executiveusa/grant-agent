# Prototype V1 runs locally and invokes installed command-line adapters.
PORT=4317
HERMES_COMMAND=hermes
GRANT_FINDER_COMMAND=grant-finder
GRANT_AGENT_ORG_ID=new-world-kids

# Never commit provider keys or organization credentials here.
# Hermes reads its provider credentials from the user's normal Hermes profile.
