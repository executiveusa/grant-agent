{
  "name": "grant-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A nonprofit funding operating agent. Software that gets you out of your seat.",
  "engines": {
    "node": ">=24.15.0"
  },
  "scripts": {
    "dev": "node --watch src/server.ts",
    "start": "node src/server.ts",
    "test": "node --test test/*.test.ts"
  }
}
